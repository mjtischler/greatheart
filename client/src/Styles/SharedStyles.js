// MT: A place to store object styles shared across components.

const SharedStyles = {
  progressLoader: {
    position: 'absolute',
    top: '44%',
    left: '48%'
  }
};

export default SharedStyles;
